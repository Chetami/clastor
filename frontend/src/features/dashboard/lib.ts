// All dashboard derivations live in @examify-tms/shared so the mobile client
// can reuse them. Re-exported here to keep existing "../lib" imports working.
export {
  previousPeriodLabel,
  currentPeriodLabel,
  formatCurrencyWhole,
  formatHours,
  deltaPercent,
  currentPeriodRange,
  plannedLessons,
  expectedIncomeFromLessons,
  findCurrentLesson,
  upcomingLessons,
  nextLesson,
  timeUntil,
  todoLessons,
  lessonChecklistTodos,
  lessonTimeRange,
  relativeDayLabel,
  buildChartData,
  sum,
  mean,
  peak,
  todayBucketLabel,
  isDenseSeries,
  findScheduleConflicts,
  type LessonChecklistItem,
  type ChartPoint,
  type ConflictPeer,
  type ScheduleConflict,
  type FindScheduleConflictsOptions,
} from "@examify-tms/shared";
