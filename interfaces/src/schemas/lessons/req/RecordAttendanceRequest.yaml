type: object
required:
  - attendanceStatus
properties:
  attendanceStatus:
    $ref: '../AttendanceStatus.yaml'
description: |-
  Request body for recording a lesson's attendance/outcome.
  Use this instead of UpdateLessonRequest to record what happened
  after the lesson (present, absent, cancelled, etc.).
