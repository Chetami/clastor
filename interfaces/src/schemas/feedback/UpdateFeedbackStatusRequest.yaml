type: object
required:
  - status
properties:
  status:
    type: string
    enum: [open, resolved]
    description: The new status for the feedback item.
description: |-
  Request body for updating a feedback item's status (resolve / reopen).
