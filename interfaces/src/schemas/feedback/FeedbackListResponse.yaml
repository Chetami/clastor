type: object
required:
  - data
  - total
properties:
  data:
    type: array
    items:
      $ref: './FeedbackResponse.yaml'
  total:
    type: integer
    minimum: 0
description: Paginated feedback list response.
