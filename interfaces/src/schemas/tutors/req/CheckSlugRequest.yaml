type: object
required:
  - slug
properties:
  slug:
    type: string
    description: Slug to check for availability against existing profiles
    example: jane-math
description: |-
  Query parameters for checking whether a profile slug is available
