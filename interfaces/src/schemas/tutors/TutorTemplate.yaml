type: string
enum:
  - classic
  - modern
description: |-
  Template identifiers for a tutor's public profile webpage.
  Determines which layout component renders the published profile.
  New templates can be added here as they are implemented.
