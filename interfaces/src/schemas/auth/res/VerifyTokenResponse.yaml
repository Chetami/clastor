type: object
required:
  - user
properties:
  user:
    $ref: './UserInfo.yaml'
description: |-
  Verify token response body
  Returned from backend /api/auth/verify endpoint
  Returns user information; a successful HTTP response indicates a valid session
